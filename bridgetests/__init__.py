"""Test-support package for the bridge behavior suite (TestPRD.md).

names    - the ONE session-name helper (run stamp, key/bare/raw forms)
live     - Bridge client for a live port, ownership + deadline teardown
baseline - per-run outcome file writer (logs/baseline/)
fakes    - fake CLI, fake git, fake clock for hermetic stories

See bridgetests/README.md.
"""
